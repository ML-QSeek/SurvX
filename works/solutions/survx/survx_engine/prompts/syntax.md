# SurvX

给ai的核心语法

## 建模规则
1. 所有 Matter（Cat、(Cat)<egoname> 、[Cat]<envname>）必须按对应分层逐项拆解定义，允许部分层级缺失。
2. `{Cat}<energyname>` 只定义数据项、数据变更事件，不定义结构、能力、关系等 Matter 属性。
3. 实例的行为由 `{Cat}<energyname>` 的 Energy 变动驱动 Matter 执行。

